

# copiare il .env da questo folder in ....
cp ~/ppdocker/DEPLOY/.env.DACOPIAREINSYMFONY /home/web/portaportese.it/.env

chown -R nobody.users /home/web/portaportese.it/


# imposta livesite come baseurl completo del sito in config services.yaml
vi /home/web/portaportese.it/config/services.yaml
# "livesite: 'https://wwwocean.portaportese.it/' "


# crea la tabella mancante

-- Drop table

-- DROP TABLE "provinceShapeDefinitivi";

CREATE TABLE "provinceShapeDefinitivi" (
	id serial NOT NULL,
	geom geometry(MULTIPOLYGON, 4326) NULL,
	cod_rip numeric NULL,
	cod_reg numeric NULL,
	cod_prov numeric NULL,
	cod_cm numeric NULL,
	cod_uts numeric NULL,
	den_prov varchar(50) NULL,
	den_cm varchar(50) NULL,
	den_uts varchar(50) NULL,
	sigla varchar(2) NULL,
	tipo_uts varchar(50) NULL,
	shape_leng numeric NULL,
	shape_area numeric NULL,
	lat numeric NULL,
	lng numeric NULL,
	lowerlat float8 NULL,
	CONSTRAINT "provinceShapeDefinitivi_pkey" PRIMARY KEY (id)
);


# esegui
docker exec -it portaportese-php php app/bin/console doctrine:schema:update --force
